import 'package:/flutter/material.dart';

class WayToSharePage extends StatelessWidget {
  const WayToSharePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}